Flask==2.2.3
# Use pre-built wheels that are compatible with your Python version
spacy==3.4.4
numpy==1.22.4
# Skip the Cython compilation by using a pre-built wheel approach
# Add explicit dependencies
wheel
requests
pytest